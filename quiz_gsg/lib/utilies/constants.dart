import 'package:flutter/material.dart';

const String quoteLink = "https://api.quotable.io/quotes/random";

const contentTextStyle = TextStyle(
    fontSize: 24, fontWeight: FontWeight.bold // Adjust the font size as needed
    );
//
